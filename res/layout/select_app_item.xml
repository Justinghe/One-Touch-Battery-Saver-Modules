<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/select_app_item"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:orientation="horizontal" >

    <ImageView
        android:id="@+id/img"
        android:layout_width="@dimen/list_iconitem_size"
        android:layout_height="@dimen/list_iconitem_size"
        android:layout_centerVertical="true"
        android:layout_margin="5.0sp" />

    <ImageView
        android:id="@+id/select_item"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentRight="true"
        android:layout_centerVertical="true"
        android:layout_marginRight="@dimen/checkbox_marginRight"
        android:gravity="right"
        android:src="@drawable/noselectitem" />

    <TextView
        android:id="@+id/title"
        android:layout_width="@dimen/list_text_width"
        android:layout_height="wrap_content"
        android:layout_centerVertical="true"
        android:layout_toLeftOf="@id/select_item"
        android:layout_toRightOf="@id/img"
        android:textSize="@dimen/explain_text_size18" />

    <ImageView
        android:id="@+id/no_select_bg"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:visibility="gone" />

</RelativeLayout>